# jsvision

> Project configuration for Claude Code. Auto-generated by /analyze_project; edit freely —
> re-running preserves your hand-written sections.

<!-- analyze_project: refreshed Toolchain (repo shape) + Project structure — packages/ui reactive core (RD-01) landed under src/reactive/; user-authored Conventions/Git/Special rules preserved -->
<!-- analyze_project: refreshed 2026-06-29 — Toolchain/Commands/Project structure for the RD-02 layout engine (packages/ui/src/layout/) + the demo:layout example; Conventions/Git/Special rules preserved -->
<!-- analyze_project: refreshed 2026-06-29 — Toolchain/Commands/Project structure for the RD-03 view/group spine (packages/ui/src/view/), its two additive primitives (runWithOwner on reactive, ScreenBuffer.clone() on core), and the demo:view example; Conventions/Git/Special rules preserved -->
<!-- analyze_project: refreshed 2026-06-30 — Project structure for the RD-04 event loop (packages/ui/src/event/) + the additive view/ surface (focusable/preProcess/postProcess, onEvent(DispatchEvent), Group.current, contract types) + the demo:events example; Conventions/Git/Special rules preserved -->
<!-- analyze_project: refreshed 2026-06-30 — Toolchain/Commands/Project structure for the RD-05 app shell (packages/ui/src/{app,desktop,window,menu,status}/) + Phase-0 additive primitives (LayoutProps absolute placement, DrawContext.role, EventLoop setCapture/onFrame, core Theme windowInactive) + the demo:shell example; Conventions/Git/Special rules preserved -->
<!-- analyze_project: refreshed 2026-06-30 (full re-scan) — Overview Type + Description now name the @jsvision/ui widget framework package (RD-01…RD-05); auto-detected toolchain/commands confirmed unchanged (yarn 1.22.22 + Turborepo, vitest, eslint+prettier, Node ≥20); Conventions/Git/Special rules preserved verbatim -->
<!-- analyze_project: refreshed 2026-07-01 — Toolchain/Commands/Project structure for the RD-06 essential controls (packages/ui/src/controls/: Text/Label/Button/Input/CheckGroup/RadioGroup + internal Cluster base + filter/range/lookup validators + shared measure.ts/charset.ts) + the additive intra-ui primitives (ev.emit/ev.focusView on DispatchEvent, per-view focus-change signal) + the new cpGrayDialog core Theme roles + the demo:controls example; Conventions/Git/Special rules preserved -->

## Overview

- **Type:** monorepo (library SDK) — published `@jsvision/core` + the `@jsvision/ui` widget framework (private until its first release) + private `@jsvision/examples`
- **Description:** SDK for building Turbo Vision-style terminal (TUI) applications in TypeScript. The **foundation** package: RD-01 scaffolding, the RD-02 **capability detection core** (`resolveCapabilities`/`resolveCapabilitiesAsync` under `src/engine/capability/`), the RD-06 **input decoder** (pure byte→event `decode`/`flush`/`createDecoderState` + `createKeymap` under `src/engine/input/`), the RD-04 **rendering engine** (width-correct `ScreenBuffer` + pure damage-diff `serialize`, glyph fallback, `sanitize`, OSC features, `cursor` under `src/engine/render/`), the RD-07 **host & lifecycle** subsystem (`createHost` native `tty` host — raw mode, alt-screen, signals, suspend/resume, guaranteed restore on every exit path, behind an injectable `RuntimeAdapter`, under `src/engine/host/`; the RD-03 real tty-backed `createTerminalQuery` lives here too — it completes RD-02's layer-2 query wiring so `resolveCapabilitiesAsync` works against a live terminal), the RD-08 **safety** subsystem (the essentials gate `evaluateEssentials`/`assertEssentials` + `detectTty`, the screen-safe `createLogger`, pure `redactEvent`/`dumpCaps`, the typed `TuiError` model, and the canonical `sanitize` injection boundary, under `src/engine/safety/`), and the RD-05 **color & styling** subsystem (depth-aware `encode`/`encodeStyle` downsampling truecolor→256→16→mono via redmean `nearest256`/`nearest16`, `styleKey`, `InvalidColorError`, DOS-16 `PALETTE` + typed `defaultTheme`, under `src/engine/color/`; it is the `serialize()` default encoder). All subsystems re-export from the single public entry point. The **`@jsvision/ui`** package is the Turbo Vision-style widget framework on `@jsvision/core` (private until its first release): the RD-01 **reactive core** (`src/reactive/`), the RD-02 **layout engine** (`src/layout/`), the RD-03 **view/group spine** (`src/view/`), the RD-04 **event loop** (`src/event/`), and the RD-05 **app shell** (`src/{app,desktop,window,menu,status}/` — `createApplication`/`run()` lifecycle, a `Desktop` window manager, `Window`/Frame chrome, overlay-hosted nested `MenuBar` menus, and a `StatusLine`), plus **Turbo Vision behavioral fidelity** (the jsvision-ui RD-10 follow-up: status emit-on-release + press capture, TV-exact cascade/tile geometry, and a functional left-grow resize gesture — all ported from the original `tdesktop.cpp`/`tstatusl.cpp`/`tframe.cpp`); see the Toolchain repo-shape note + Project structure for the per-subsystem detail. The RD-03 **capability probe & survey harness** is a dev-only diagnostic under `examples/capability-probe/` (run via `npm run probe`) — auto + guided-manual probes, a live input/mouse readout, a JSON + table report, and a checked-in `terminal-matrix.json` evidence base; it is **not** part of the published package. The RD-09 **testing strategy & acceptance gate** realizes the four test tiers (data-driven input corpus, `@xterm/headless` golden-screen across all four colour depths, no-node-pty Tier-3 restore-on-every-exit, seeded decoder fuzz + byte-proportionality) and the **project go/no-go gate** (`npm run gate` → `scripts/gate.mjs` + `docs/acceptance-gate.md`), with cross-platform/manual-matrix/real-PTY-resize/wall-clock-perf concerns recorded DEFERRED (DEF-1…DEF-4). The RD-10 **non-functional baseline** adds the realized/verified cross-cutting NFRs: a frame-performance **bench** (`npm run bench` → `bench/frame-bench.mjs`, median/p95 for the 200×50 compose+diff) with a 16 ms ceiling spec that auto-skips its hard assertion under `CI`/`TUI_SKIP_PERF` (resolving RD-09 DEF-4), an **esbuild** dev-only tree-shake check (one-symbol ≪ all-exports bundle), a **detection-budget** test (non-responding query falls back within `timeoutMs`), **NO_COLOR/ASCII-fallback golden** tests, **API governance** (`CHANGELOG.md` + a README "Versioning & stability" policy), and a **techdocs** architecture/API-reference/ADR set under `docs/` (VitePress-compatible, not installed). Much of RD-10 is **mapped, not rebuilt** (output∝damage, bounded buffers, cross-platform CI, packaging contract). Deferred: DEF-1 npm provenance · DEF-2 dependency-license guard · DEF-3 typed-array buffer backing.

## Toolchain

- **Repo shape:** **yarn 1.x + Turborepo monorepo**. `packages/core` = published `@jsvision/core` (the foundation engine, zero runtime deps); `packages/examples` = private `@jsvision/examples` (dev examples + probe harness); `packages/ui` = `@jsvision/ui` (the Turbo Vision-style widget framework on `@jsvision/core`; `private` until its first release — landed so far: the RD-01 **reactive core** `src/reactive/`, the RD-02 **layout engine** `src/layout/`, the RD-03 **view/group spine** `src/view/` — the retained `View`/`Group` widget tree binding reactivity + layout into a self-drawing, themed, independently renderable frame — the RD-04 **event loop** `src/event/` (the host-agnostic `createEventLoop`/`EventLoop` dispatch mechanism: 3-phase dispatch, focus chain + Tab, mouse hit-test, typed commands + keymap, async modality; drives the RD-03 `RenderRoot` one frame per tick), and the RD-05 **app shell** `src/{app,desktop,window,menu,status}/` (the `createApplication`/`run()` lifecycle, a `Desktop` window manager, `Window`/Frame chrome, overlay-hosted nested `MenuBar` menus, and a `StatusLine` — a full Turbo Vision-style application over the RD-04 loop), and the RD-06 **essential controls** `src/controls/` (the Tier-1 leaf widgets `Text`/`Label`/`Button`/`Input`/`CheckGroup`/`RadioGroup` over an internal `Cluster` base + the `filter`/`range`/`lookup` validator model, faithfully ported from Turbo Vision `t*.cpp`/`tvalidat.cpp`; two additive intra-ui primitives — `ev.emit`/`ev.focusView` on `DispatchEvent` + a per-view focus-change signal — and the additive `cpGrayDialog` core `Theme` roles)). Future packages: `@jsvision/<name>` under `packages/<name>/`. All **public** packages share one lockstep version (`yarn sync-versions`; root `package.json#version` is the source of truth).
- **Language(s):** TypeScript (ESM-only, `module`/`moduleResolution` NodeNext, `strict`); shared `tsconfig.base.json`, per-package `tsconfig.json`
- **Framework(s):** none — Node built-ins + zero runtime dependencies
- **Package manager:** **yarn 1.x** (`yarn.lock`, workspaces `packages/*`)
- **Orchestration:** **Turborepo** (`turbo.json`: `build`/`typecheck`/`test`/`test:e2e`/`check:deps`; `test` dependsOn `build`, `typecheck` dependsOn `^build`)
- **Test framework:** **vitest** (two projects: `unit` = `*.{spec,impl}.test.ts`, `e2e` = `*.e2e.test.ts` single-fork). Vite resolves NodeNext `.js`→`.ts` natively. Tests use `expect()`; `tsx` is retained for bench/examples/probe + e2e child processes.
- **Lint/format:** root-global ESLint flat config (`typescript-eslint`) + Prettier
- **Node:** active LTS **20 / 22 / 24** (`engines.node >= 20`; Node 18 dropped, EOL)

## Commands

> All commands run from the monorepo root and fan out via turbo unless noted.

- **Verify (run before every commit):** `yarn verify` (= `turbo run typecheck build test` across packages)
- **Build:** `yarn build` (`turbo run build` → each package's `tsc` → `dist/`; `core` + `ui` build, examples don't)
- **Typecheck:** `yarn typecheck` (`turbo run typecheck`; `^build` so examples typecheck against core's `.d.ts`)
- **Test (unit):** `yarn test` (`turbo run test` → vitest `unit` project per package)
- **Test (e2e):** `yarn test:e2e` (`turbo run test:e2e` → vitest `e2e` project) · or per package: `yarn workspace @jsvision/core test:e2e` (restore/signals/install) · `yarn workspace @jsvision/examples test:e2e` (probe)
- **Run the probe harness (dev):** `yarn workspace @jsvision/examples probe` (`--auto`/`--out <path>`/`--no-matrix`/`--help`)
- **Runnable demos (dev):** `yarn workspace @jsvision/examples <demo>` — `demo:controls` (RD-06 essential controls — headless form walkthrough: a `Label`-linked `Input` with a live `filter` reject, `CheckGroup`/`RadioGroup` toggles, and a default `Button` emitting its command via `ev.emit`, ASCII frame per step), `demo:shell` (RD-05 app shell — headless WM walkthrough: open 3 windows → raise/drag/zoom → tile → F10 menu Cascade → F4 status Tile, ASCII frame per step), `demo:events` (RD-04 event loop — headless synthetic-dispatch walkthrough: Tab focus, an `'ok'` command, an async `execView`/`endModal` modal, ASCII frame per step), `demo:view` (RD-03 view/group spine — a themed app-shell tree composed + reflowed + reactively repainted, ASCII render), `demo:layout` (RD-02 layout walkthrough + ASCII render), `demo:reactive` (RD-01 reactive core), `demo:resize` / `demo:playground` (real-TTY host demos)
- **Acceptance gate (RD-09 go/no-go):** `yarn gate` (`scripts/gate.mjs`, root) — runs `yarn verify` + core e2e + examples `probe --auto`, PASS/FAIL/DEFERRED per criterion; map in `docs/acceptance-gate.md`
- **Performance bench (RD-10, informational):** `yarn bench` (`yarn workspace @jsvision/core bench` → `tsx bench/frame-bench.mjs`); never gates (16 ms ceiling asserted off-CI by `packages/core/test/perf-budget.spec.test.ts`, skipped under `CI`/`TUI_SKIP_PERF`)
- **Version sync (lockstep):** `yarn sync-versions` (write root version to public packages) · `yarn sync-versions --check` (assert lockstep, non-zero on drift)
- **Lint:** `yarn lint` (`eslint .` + `prettier --check .`, repo-wide) · **Fix:** `yarn lint:fix`
- **Dependency policy:** `yarn check:deps` (`turbo run check:deps`; fails on any native runtime dependency per public package)
- **Clean:** `rm -rf packages/*/dist .turbo`

## Project structure

```
packages/core/       Published @jsvision/core — holds src/engine/, bench/, and the 80 non-probe tests + fixtures. Paths below (src/engine/**, test/**) are RELATIVE TO THIS PACKAGE.
packages/examples/   Private @jsvision/examples — capability-probe/ + resize-demo/ + the 15 probe-*/probe.e2e tests; depends on @jsvision/core, imports it by name.
packages/ui/             @jsvision/ui — the Turbo Vision-style widget framework (retained tree + signals) on @jsvision/core. Single public entry src/index.ts re-exports each landed subsystem (layout + view + event + app-shell use EXPLICIT named re-exports; reactive uses export *): src/reactive/ (RD-01 reactive core), src/layout/ (ADR-008/RD-02 cell-native layout engine), src/view/ (RD-03 view/group spine), src/event/ (RD-04 event loop), and the RD-05 app shell src/{app,desktop,window,menu,status}/ — all see below. private until first release; remaining subsystems land per codeops/features/jsvision-ui/plans/tui-ui/01-component-map.md.
packages/ui/src/layout/    RD-02 layout engine (ADR-008) — cell-native, integer-correct flex. apportion.ts (the de-risking spike: largest-remainder apportion + 1-D solveTrack, unchanged), types.ts (node model Size2D/Rect/Padding/Size/Justify/Align/Direction/LayoutProps/LayoutBox/LayoutResult + internal prop normalizers + axis projection helpers), measure.ts (naturalSize intrinsic sizing — measure() leaf or derived-from-children), layout.ts (the recursive two-axis pass), index.ts (barrel). Public API: layout(root, viewport) → Map<LayoutBox, Rect> of parent-relative integer rects; row/col via one axis abstraction, fixed/fr/auto sizing, justify/align/gap/padding, overflow (extend past edge, fr→0), degenerate→zero rects; pure/no-mutation. Tests: packages/ui/test/layout.{sizing,align,tree,packaging}.{spec,impl}.test.ts (18 ST oracles ST-01…ST-18 ↔ AC-1…AC-18 + impl). Plan: codeops/features/jsvision-ui/plans/layout-engine/.
packages/ui/src/reactive/  RD-01 reactive core — fine-grained Solid-style signals (UI-independent). 12 files ≤ 500 lines, single barrel index.ts: types (Signal/Computed/EqualsOption public + Source/Computation/Owner/Subscribable/NodeState internal), errors (ReactiveCycleError extends @jsvision/core TuiError), warnings (NODE_ENV-gated devWarn), cleanup (LIFO drain), scheduler (tracking context + two-phase glitch-free lazy-pull propagation, batch/untrack, 1000-iteration runaway guard, multi-throw drain), owner (createRoot/onCleanup/dispose + no-owner policy), signal, computed (lazy+memoized), effect, show (Show), for (For — keyed reconcile + reactive index). Public API: signal/computed/effect/batch/untrack/onCleanup/createRoot/Show/For/ReactiveCycleError, **+ the RD-03 additive primitive runWithOwner(owner, fn) + getOwner() + opaque Owner** (explicit-parent owner-scope nesting). Tests: packages/ui/test/reactive.*.{spec,impl}.test.ts (20 ST oracles ST-01…ST-20 + impl; runWithOwner = ST-21 in reactive.ownership). Plan: codeops/features/jsvision-ui/plans/reactive-core/.
packages/ui/src/view/      RD-03 view/group spine — the retained widget tree binding RD-01 reactivity + RD-02 layout into a self-drawing, themed, independently renderable frame on @jsvision/core's ScreenBuffer. 8 files ≤ 231 lines + barrel index.ts (layout + view use EXPLICIT named re-exports in src/index.ts): geometry (Point + pure intersect/translate/contains), types (ViewState/DrawContext/ThemeRoleName=keyof Theme/RenderRootOptions), theme-style (themeRoleToStyle {fg,bg} adapter), view (abstract View — bounds/state flags/onEvent stub/bind/invalidate/invalidateLayout/onMount/onCleanup + internal ViewHost seam + mount/unmount; mount untracks its scope setup so onCleanup binds to the view's own scope), group (Group — children/background/add/remove/addDynamic over Show/For + recursive mount), draw-context (makeDrawContext — stateless clipped view-local paint, all writes via ScreenBuffer+sanitize), reflow (view tree → fresh LayoutBox tree → RD-02 layout() → parent-relative bounds; visible:false omitted), render-root (createRenderRoot/RenderRoot — buffers + ScreenBuffer.clone() snapshot, caps, theme, injectable scheduler, dirty-set + per-view compose-context cache, compose walker with clip/back-to-front/bg/draw-error isolation, full-vs-partial recompose). Public API: View/Group/DrawContext/RenderRoot/createRenderRoot/Point/ViewState/ThemeRoleName/intersect/translate/contains (+ runWithOwner/getOwner/Owner via reactive). Tests: packages/ui/test/view.{tree,drawcontext,reflow,render,scheduler,dynamic,packaging}.{spec,impl}.test.ts (ST-01…ST-20 ↔ AC-1…AC-20 + ST-21/22 primitives + impl) + examples view-demo.e2e. Plan: codeops/features/jsvision-ui/plans/view-group-spine/ (runtime decisions RT-1…RT-5 in its 00-ambiguity-register.md). The core-side primitive: ScreenBuffer.clone() added to packages/core/src/engine/render/buffer.ts (exact deep copy incl. wide-lead/continuation cells) for the partial-recompose damage diff.
packages/ui/src/event/     RD-04 event loop — the host-agnostic dispatch mechanism on the RD-03 spine (no cross-package primitive; builds on existing @jsvision/core + RD-03). 8 files ≤ 227 lines + barrel index.ts (EXPLICIT named re-exports in src/index.ts): types (EventLoop/EventLoopOptions; re-exports the CommandEvent/AppEvent/DispatchEvent contract types declared in ../view/types.ts — PA-8, no view/↔event/ cycle), event-loop (createEventLoop — builds+OWNS the RenderRoot with a DEFERRING schedule so the loop drives flush() itself; the single runTick shared by EVERY public mutator → one coalesced frame per tick + onIdle + handler-error isolation, PA-11; dispatch/resize/getFocused/focus*/emitCommand/enable*/execView/endModal; routeContext seam builder), dispatch (route(ev,ctx) — keymap consume PA-1 → built-in Tab/Shift-Tab PA-10 → mouse/wheel branch → pre/focus/post 3-phase sweeps with handled short-circuit + focus-chain bubble clamped to scopeRoot PA-12), commands (createCommandRegistry — emit/enable/isEnabled, enabled-by-default explicit-override map PA-3, enqueues CommandEvent onto the tick), focus (createFocusManager — per-group current chain, isFocusable predicate + subtree semantics AR-65, focusView/focusInto/focusNext/focusPrev wrap+descend, save/restore via persisted current; pure mutations, the loop wraps each in runTick), hit-test (hitTestRoute — 1-based→0-based normalize AR-63, top-most-first walk reverse-z + ancestor-clip + skip !visible/disabled, focus-on-click climb, view-local ev.local), modal (createModalManager — execView/endModal stack, save/restore-around-modal, nested LIFO, scopeRoot capture). Public API: createEventLoop/EventLoop/EventLoopOptions/CommandEvent/AppEvent/DispatchEvent. Additive RD-03 edits: view/view.ts (focusable/preProcess/postProcess + onEvent(DispatchEvent) retype), view/group.ts (internal current), view/types.ts (contract types). Tests: packages/ui/test/event.{loop,dispatch,focus,mouse,modal}.{spec,impl}.test.ts + event.packaging.spec (20 ST oracles ST-01…ST-20 ↔ AC-1…AC-20 + impl) + examples event-demo.e2e. Plan: codeops/features/jsvision-ui/plans/event-loop/ (PA-1…PA-12, runtime notes in 00-ambiguity-register.md).
packages/ui/src/{app,desktop,window,menu,status}/  RD-05 app shell — a full Turbo Vision-style application over the RD-04 loop. EXPLICIT named re-exports in src/index.ts. app/ (createApplication — composes the loop + an owned Desktop + optional MenuBar/StatusLine + an absolute full-viewport overlay + a hidden quit-command sink into a column; run() in run.ts wires createHost → dispatch, loop.onFrame = host.render, restore on every exit path). desktop/ (Desktop window manager — postProcess; windows() type-guard, raise/cascade/tile/focusNext-prev/Number, beginMove/beginResize/beginResizeLeft via the loop's setCapture pointer-capture seam; handleCommand returns bool + sets ev.handled to avoid double-processing; gestures.ts clamp/applyMove/applyResize/applyResizeLeft + arrange.ts TV-exact cascade/tile (tdesktop.cpp doCascade + iSqr/mostEqualDivisors/dividerLoc/calcTileRect ported verbatim, tileError→no-op)). window/ (Window extends Group — focusable, absolute, reactive title Signal, movable/resizable/zoomable/closable, restoredRect; drawFrame chrome — border + close [×]/zoom [↑]|[↕]/number + SE (─┘) & SW (└─) resize grips + frameZoneAt hit zones incl. the functional left-grow resize-left (TV dmDragGrowLeft); a WindowManager back-reference seam breaks the window↔desktop import cycle). menu/ (overlay-hosted nested menus — builders.ts menuBar/subMenu/item/separator + tilde ~X~ parseTilde/layoutTitles; controller.ts nav state machine — level stack of MenuPopups + a full-viewport outside-click catcher, ↑↓ skip separators/disabled, Enter activate, ←→ switch/open-sub, Esc one level, hotkeys, focus save/restore, overlay.visible gated on menu state PF-10; menubar.ts MenuBar pre-process — F10/Alt-hotkey/click open; popup.ts MenuPopup presentational + onPick row-click). status/ (StatusLine post-process command row — statusLine/statusItem builders, attach(seam), press-capture feedback + emit-on-release (command of the item under the release point), tildeSegments hotkey runs, matchesChord accelerator parser, greying; commands.ts Commands constants quit/close/zoom/next/prev/cascade/tile). Phase-0 additive primitives: RD-02 LayoutProps {position:'absolute',rect}, RD-03 DrawContext.role, RD-04 EventLoop setCapture/releaseCapture/onFrame, the cross-package edits = core Theme windowInactive + statusSelected roles. Tests: packages/ui/test/app-shell.{lifecycle,desktop,window,menu,status,seams,packaging}.{spec,impl}.test.ts (22 ST oracles ST-01…ST-22 ↔ AC-1…AC-22 + impl) + examples shell-demo.e2e. Plan: codeops/features/jsvision-ui/plans/app-shell/ (PA-1…PA-19, runtime notes in 00-ambiguity-register.md). **Turbo Vision behavioral fidelity** (the jsvision-ui-RD-10 follow-up: status emit-on-release + press capture, TV-exact cascade/tile geometry, functional left-grow resize) layered over these subsystems — plan codeops/features/jsvision-ui/plans/tv-behavioral-fidelity/ (PA-1…PA-11; rewrites the desktop ST-11 oracle → ST-05/06 + the status emit oracles).
packages/ui/src/controls/  RD-06 essential controls — the Tier-1 leaf widgets + validator model, faithfully ported from Turbo Vision. EXPLICIT named re-exports in src/index.ts. text.ts (Text — static multi-line paragraph; greedy word-wrap wrapText + hard-break, `staticText` role; tstatictx.cpp). label.ts (Label — postProcess hotkey label linked to a control; parseTilde/tildeSegments reused from ../menu/, ev.focusView on click/hotkey, repaints on the linked view's focus-change signal; tlabel.cpp). button.ts (Button — `[ text ]` face + ▄/█/▀ block-glyph shadow in the `shadow` role, state→faceRole, drawTitle markers, activate→ev.emit(command); tbutton.cpp). input.ts (Input — single-line editor over a two-way Signal<string>, curPos/firstPos scroll with ◄/► arrows matching tinputli.cpp, insertPrintable gated by an optional validator's isValidInput, valid() exposed; tinputli.cpp). cluster.ts (internal abstract Cluster base — ClusterBox 5-cell box, mark/press/box/movedTo abstract, ↑↓/Space/Alt-hotkey/click nav + moveSel; tcluster.cpp). check-group.ts (CheckGroup extends Cluster — Signal<boolean[]>, ` [ ] `/`X` box; tcheckboxes.cpp) + radio-group.ts (RadioGroup extends Cluster — Signal<number>, ` ( ) `/`•` box, movedTo sets value; tradiobut.cpp). measure.ts (shared glyphWidth/stringWidth, WIDTH_MODE='wcwidth'). validators/ ({isValidInput,isValid,error?} model — types, charset (expandCharSet range-aware + allInSet), filter (allowed-charset live reject), range (TV-faithful unsigned/signed validChars + INTEGER+min/max bounds; tvalidat.cpp), lookup (allowlist membership), index barrel). Additive primitives: RD-04 ev.emit/ev.focusView on DispatchEvent (sourced from RouteContext, enriched once in route() as ev2) + a per-view lazy focusSignal() (PF-009, poked by focus.ts focusLeaf); core: the cpGrayDialog Theme roles staticText/label/labelSelected/labelShortcut/button*/cluster*/input* in packages/core/src/engine/color/theme.ts. One runtime decision PA-15 (TV-faithful range validChars) in the plan's 00-ambiguity-register.md. Tests: packages/ui/test/controls.{foundation,text-label,button,validators,input,cluster,focus,packaging}.{spec,impl}.test.ts (ST-01…ST-16 + impl) + examples controls-demo.e2e. Plan: codeops/features/jsvision-ui/plans/essential-controls/.
tsconfig.base.json  turbo.json  vitest.config.ts(per package)  — shared TS config, turbo pipeline, vitest unit+e2e projects.
docs/  scripts/  .github/  CHANGELOG.md  — monorepo-level docs (techdocs + acceptance-gate), scripts (gate.mjs, check-no-native-deps.mjs, sync-versions.mjs), CI, changelog.
--- (the subsystem layout below is rooted at packages/core/) ---
src/engine/      Source. Single public entry point: src/engine/index.ts (re-exports public API).
src/engine/capability/   RD-02 capability detection core (profile, defaults, env, table, query, detect, index) + responses.ts (RD-06-shared query-response classifier).
src/engine/input/        RD-06 input decoder (events, keys, decoder, mouse, paste, keymap, index).
src/engine/render/       RD-04 rendering engine (types, width, buffer, ansi, glyphs, serialize, sanitize, cursor, osc, index).
src/engine/host/         RD-07 host & lifecycle (types, streams, platform, modes, host, restore, signals, index) — native tty host behind an injectable RuntimeAdapter. streams.ts also exports the additive detectTty() pre-start TTY probe (RD-08 PF-001); terminal-query.ts is the RD-03 real tty-backed createTerminalQuery (layer-2 query seam).
src/engine/safety/       RD-08 safety (sanitize, errors, redact, logger, essentials, index) — essentials gate, screen-safe logger, redaction, typed errors, and the canonical injection boundary.
src/engine/color/        RD-05 color & styling (color, palette, downsample, encode, theme, index) — depth-aware SGR encoding (truecolor→256→16→mono), redmean nearest-color, DOS-16 palette + theme (incl. the jsvision-ui-RD-10 statusSelected pressed-item role); the serialize() default encoder.
bench/           (under packages/core/) RD-10 frame-performance benchmark (frame-bench.mjs) behind `yarn bench` — exported pure median/p95/measureComposeDiff helpers + a main-guarded printing CLI; never emitted to dist.
test/            (under packages/core/, 76 files) RD-09 four-tier strategy: hex-in-JSON input corpus (fixtures/input-corpus/ + input-corpus.spec/impl + -helpers), golden-screen via @xterm/headless (golden-screen.spec/impl + -helpers), Tier-3 host-tier3.e2e, seeded fuzz (input-fuzz.spec/impl + -helpers), bytes∝damage (render-bytes-damage.spec), gate consistency (gate.spec). RD-10: perf-budget.spec/impl, treeshake.spec, a11y-golden.spec, api-stability.spec, docs-presence.spec. Monorepo: sync-versions.spec. Governance specs (gate/check-deps/docs-presence/api-stability/toolchain) reach the monorepo root via ../../.. (DEF-4 to extract). Shared helpers in non-test *-helpers.ts.
packages/examples/   (capability-probe/ = foundation-RD-03 probe harness: main/args/taxonomy/env-meta/auto-probes/manual-probes/live-readout/report/matrix; resize-demo/, keyboard-mouse-playground/, reactive-demo/ = RD-01 walkthrough, layout-demo/ = RD-02 walkthrough + ASCII render, view-demo/ = RD-03 view/group spine walkthrough + ASCII render, event-demo/ = RD-04 event-loop walkthrough — focus + command + modal, ASCII render, shell-demo/ = RD-05 app-shell walkthrough — windows + WM + menu + status, ASCII render, controls-demo/ = RD-06 essential-controls walkthrough — a Label-linked Input + live filter, CheckGroup/RadioGroup, default Button command, ASCII render) + test/ holds the probe-*/probe.e2e tests + view-demo.e2e + event-demo.e2e + shell-demo.e2e + controls-demo.e2e (demo child runs via tsx). Imports @jsvision/core and @jsvision/ui by name; run via the demo:* / probe scripts.

docs/            Monorepo docs (root). RD-09 acceptance-gate.md (criteria→evidence). RD-10 techdocs set (VitePress-compatible, NOT installed): index.md (techdocs:true), architecture/ (system-overview, api-design, security), decisions/ (ADR-001…007 incl. the monorepo restructure), guides/ (getting-started, development), .vitepress/config.ts.
scripts/         (root) check-no-native-deps.mjs (dependency-policy guard, takes a package dir), gate.mjs (RD-09 aggregator behind `yarn gate`, cross-package), sync-versions.mjs (lockstep version sync behind `yarn sync-versions`).
.github/workflows/ci.yml   CI matrix: 3 OS × Node 20/22/24 (yarn install --frozen-lockfile → turbo verify; POSIX e2e per package; check:deps; sync-versions --check; audit; pack).
packages/*/dist/  Build output (gitignored). Generated by each package's tsc.
codeops/         CodeOps nested layout (marker .codeops.yml: codeopsLayout=nested, rdIdScope=per-feature). 00-roadmap.md = portfolio roadmap; features/<slug>/ holds one feature-set's 00-roadmap.md + requirements/ + plans/<plan>/. Active feature: features/jsvision-ui/ (requirements RD-* + plans layout-engine, reactive-core, tui-ui, view-group-spine, event-loop, app-shell, tv-behavioral-fidelity, essential-controls). Archived under codeops/_archive/: foundation/ (the completed RD-01…RD-10 feature-set, incl. its requirements/) and monorepo-restructure/ (the completed repo-monorepo conversion). Source-relative links from a plan reach the repo root via ../../../../../.
_archive/        Archived Ink/React prototype — reference/inspiration only, not built (distinct from codeops/_archive/).
terminal-matrix.json       RD-03 cross-terminal evidence (appended by the probe / gate); untracked + gitignored dev-box artifact.
```

**Test files:** under each package's `test/`. Naming: `*.spec.test.ts` (specification — immutable oracle from requirements/AC), `*.impl.test.ts` (implementation/edge cases), `*.e2e.test.ts` (end-to-end, vitest `e2e` project). core tests import source via `../src/engine/...`; examples tests import the engine via `@jsvision/core`.

<!-- analyze_project: refreshed Overview, Toolchain, Commands, Project structure for the yarn+turbo+vitest monorepo (2026-06-28) -->
<!-- analyze_project: refreshed Project structure + plan-path references for the nested codeops/ layout migration; synced .prettierignore note (2026-06-29) -->

## Conventions

- **ESM-only**, zero runtime dependencies (pure-JS only; the `check:deps` guard enforces no native deps).
- **Foundation-first layering** with a single public entry point (`src/engine/index.ts`); every later RD adds modules under `src/engine/**` and re-exports public symbols there. Target 200–500 lines per file.
- **NodeNext import specifiers** use the `.js` extension even for `.ts` sources (e.g. `from './version.js'`).
- **Tests in `test/` only** — do not colocate tests with source.
- Public/exported symbols carry JSDoc (purpose, params, returns, side effects).
- **Grounded Options & Recommendations** — follow the always-on directive in the coding standards: filter out non-viable options (no strawmen), second-guess each, ground any code-modifying option in the real code, and lead with a recommendation and its reason; match ceremony to stakes.

## Git conventions

- **Commit scope:** the area touched — `scaffold`, `package`, `toolchain`, `packaging`, `tests`, `docs`, or the engine subsystem name in later RDs.
- **Main branch:** `master` • **Remote:** `origin` → `git@github.com:blendsdk/tui.git`. Publish (with provenance) is still deferred to a later milestone (RD-10 DEF-1).

## Turbo Vision fidelity (NON-NEGOTIABLE)

> `@jsvision/ui` is a faithful re-creation of Borland Turbo Vision. **Do not reimagine the
> drawing.** For any component, widget, or chrome that has a Turbo Vision counterpart, you MUST
> first read the original source and replicate **their** geometry, glyphs, sizing, layout, and
> hit-zones exactly.

- **Source of truth:** the original Turbo Vision (magiblot/tvision) checked out at
  `/home/gevik/workdir/github/tvision` — `source/tvision/t*.cpp` (drawing/sizing),
  `source/tvision/tvtext1.cpp` (the `frameChars`/glyph tables), `include/tvision/*.h` (geometry).
- **Process for every TV-derived component** (before writing or changing its draw/size/layout code):
  1. Locate the original class (e.g. `TMenuBox`, `TMenuBar`, `TFrame`, `TButton`, `TScrollBar`,
     `TWindow`, `TDialog`) and read its `draw()`, sizing (`getRect`/`sizeLimits`), `getItemRect`,
     and the glyph tables it uses.
  2. Replicate the **exact** column math, frame/gutter insets, padding, fill characters, markers,
     and hit-zones. Convert CP437 byte glyphs to their Unicode equivalents (mind East-Asian
     ambiguous-width — see the close-glyph note: prefer unambiguous-narrow code points).
  3. Cite the original file(s) in the code's JSDoc and in the commit message.
- **No invention.** If the original is unclear or a detail isn't covered, surface it and ask —
  never substitute your own design for theirs. This is proven (the RD-05 menu box matched
  `tmenubox.cpp` 1:1); every component from here on out honors the same rule.
- This governs **drawing/geometry**. Behavior the original couldn't have (truecolor, reactive
  binding, async modality) may extend TV, but the visual shapes/sizes must still match.

## Special rules

- **Spec-first task ordering** (CodeOps): spec tests → red → implement → green → impl tests → verify. A `*.spec.test.ts` is an immutable oracle — if it fails after implementation, fix the code, not the test.
- `npm audit` is **not** wired into `verify` (it runs in CI and is locally runnable) so audit advisories don't block local typecheck/test loops.
- Prettier is scoped to code: `codeops/` is in `.prettierignore` (CodeOps process docs, not code).

<!-- analyze_project: generated Toolchain, Commands, Project structure, Conventions, Git conventions, Special rules -->
<!-- analyze_project: refreshed 2026-06-27 — Overview + Project structure for the RD-02 capability subsystem (src/engine/capability/). Toolchain/Commands unchanged. -->
<!-- analyze_project: refreshed 2026-06-27 — Overview + Project structure for the RD-06 input subsystem (src/engine/input/ + capability/responses.ts). Toolchain/Commands unchanged. -->
<!-- analyze_project: refreshed 2026-06-27 — Overview + Project structure for the RD-04 rendering engine (src/engine/render/). Toolchain/Commands unchanged. -->
<!-- analyze_project: refreshed 2026-06-30 — Overview + Project structure for jsvision-ui RD-10 (Turbo Vision behavioral fidelity: status emit-on-release/press-capture, TV-exact cascade/tile, left-grow resize; new statusSelected core role + tv-behavioral-fidelity plan; core tests 76→80; corrected the stale frame-chrome glyphs to [×]/[↑]|[↕]/grips). Toolchain/Commands unchanged. -->
