# Ambiguity Register: RD-06 Input System (Plan)

> **Status**: ✅ GATE PASSED — all 13 items resolved
> *(User confirmed via the make_plan interview on 2026-06-27: 10 explicit choices across three question rounds + "Confirm all" for the PL-11…PL-13 planner recommendations.)*
> **Last Updated**: 2026-06-27
> **Scope**: Plan-level decisions for implementing **RD-06** (input system / pure byte→event decoder) only.
> **Source RD**: [RD-06](../../requirements/RD-06-input-system.md)

The requirements-level Zero-Ambiguity Gate (AR-* in the requirements set) already
passed. This register covers only the **new** plan-level decisions surfaced while
turning RD-06 into an implementation plan. Each entry is prefixed `PL-`.

| #     | Category | Ambiguity / Gap | Options Presented | User Decision | Status |
|-------|----------|-----------------|-------------------|---------------|--------|
| PL-1  | Architecture / Types | Decoder input unit — the RD's `decode(buffer: Bytes, state)` vs the prototype's JS `string` | A: `Uint8Array` · B: `string` | **A — `decode(bytes: Uint8Array, state)`. Only raw bytes are chunk-boundary-safe: a UTF-8 multibyte char or a CSI sequence split across two stdin chunks cannot be represented mid-codepoint by a JS string. Matches RD-02's byte-level `query.ts`. UTF-8 decoding happens inside the decoder once a printable is complete.** | ✅ Resolved |
| PL-2  | Architecture / DRY | RD-06's `decode()` must recognise query responses (DA/`?2026`/XTVERSION) and route them as `QueryResponse` (AC-6); RD-02's `query.ts:192-253` already parses these grammars | A: share matchers (extract + small `query.ts` refactor) · B: duplicate in the decoder | **A — extract the response-grammar classifier into one shared module (`src/engine/capability/responses.ts`); both `query.ts` (RD-02) and the new decoder import it. `query.ts` behaviour is unchanged; its immutable spec tests are the refactor safety net. One source of truth.** | ✅ Resolved |
| PL-3  | Architecture / Purity | The decoder must be a pure function (corpus-testable) but lone-`ESC` vs sequence-start needs a ~50 ms timeout — a pure function cannot hold a timer (same tension as RD-02's RT-2) | A: pure decoder + host-driven `flush()` · B: async decoder owning a timer | **A — `decode()` stays pure: an ambiguous trailing `ESC` is held in state/`rest`. A separate `flush(state)` (called by the RD-07 host when its ~50 ms timer fires) forces the held `ESC` out as a standalone Escape key. Timing lives in the host; RD-06 ships the seam + the `ESC_TIMEOUT_MS = 50` default. Mirrors RD-02's RT-2 sync/async split.** | ✅ Resolved |
| PL-4  | Scope | Must-Have lists CSI-u/Kitty decoding, but the roadmap schedules "CSI-u/Kitty enhancement" in Phase B and none of AC-1…AC-8 test it (all ACs are classic xterm) | A: classic now, defer CSI-u to Phase B · B: implement CSI-u now too | **A — fully implement classic xterm decoding (satisfies the gate + every AC). Wire the `caps.keyboard.kittyFlags` branch point + classic fallback now, but defer the actual CSI-u/Kitty sequence parsing to Phase B (tracked **DEF-1**). All 8 ACs are classic; the roadmap explicitly schedules CSI-u in Phase B.** | ✅ Resolved |
| PL-5  | Security / Behavior | Bracketed-paste size cap default (RD-08 owns the value later but isn't built) | A: 1 MiB · B: 64 KiB | **A — `PASTE_CAP_BYTES = 1_048_576` (1 MiB), configurable via decoder options. Matches the RD-06 security note's example. On exceed: deliver text up to the cap with `truncated:true`, then discard remaining bytes until `ESC[201~`.** | ✅ Resolved |
| PL-6  | Security / Behavior | Bound on the incomplete-sequence carry buffer (non-paste `rest`) to stop an adversarial unterminated CSI from growing memory (AC-7/AC-8) | A: 1024 bytes (reuse RD-02's cap) · B: 256 bytes | **A — reuse RD-02's `RESPONSE_BUFFER_CAP = 1024` (`query.ts:23`) so the codebase has one consistent terminal-bytes limit. Far above any real (<~20 byte) sequence; on overflow, drop the carry and resync (emit nothing for the garbage).** | ✅ Resolved |
| PL-7  | Scope / Should-have | Which RD-06 Should-Have items to include now | (multi-select) Focus events · Pluggable keymap | **BOTH — include focus in/out events (`CSI I`/`CSI O`, gated on `?1004`) and the pluggable keymap in this plan. Neither is deferred.** | ✅ Resolved |
| PL-8  | Layout | Module layout for the decoder | A: `input/` subfolder + shared classifier in `capability/` · B: flat under `src/engine/` | **A — new `src/engine/input/` (`events`, `decoder`, `keys`, `mouse`, `paste`, `keymap`, `index`); shared query-response classifier extracted to `src/engine/capability/responses.ts` (imported by the decoder). Public API re-exported from `src/engine/index.ts`. Matches PL-4's `capability/` precedent from RD-02.** | ✅ Resolved |
| PL-9  | API | How `decode()`'s return models the query-vs-app separation (AC-6: query responses reach the capability channel, zero app keystrokes) | A: separate field `{ events, queries, rest }` · B: single union, host filters | **A — `decode()` returns `{ events: InputEvent[], queries: QueryResponse[], rest: Uint8Array }`. Query responses are structurally separate from app `events`; the host routes `queries`→RD-02 and `events`→app. A query response physically cannot appear in the app stream (structural AC-6 guarantee).** | ✅ Resolved |
| PL-10 | API | Pluggable keymap shape | A: functional `createKeymap(bindings) → lookup(KeyEvent)` · B: class-based `Keymap` | **A — `createKeymap({'ctrl+s':'save', …})` returns a pure `lookup(KeyEvent) => name | undefined`. Chord grammar: optional mods (`ctrl`/`alt`/`shift`) + a key name, lowercased, `'+'`-joined. Stateless, pure, matches this RD's pure-function ethos.** | ✅ Resolved |
| PL-11 | API / Naming | Event-model field shapes & discriminator | (recommendation) | **Discriminator `type: 'key'|'mouse'|'wheel'|'paste'|'focus'`; `MouseEvent` keeps the RD's `kind: 'down'|'up'|'move'|'drag'` sub-discriminator. `KeyEvent { type, key, ctrl, alt, shift, codepoint? }` (printable → `key` is the char + `codepoint`; named → lowercase name). `MouseEvent { type, kind, button, x, y }` with **1-based coords as the terminal reports** (AC-3 `240` stays `240` — differs from the 0-based prototype). `WheelEvent { type, dir, x, y }`, `PasteEvent { type, text, truncated }`, `FocusEvent { type, focused }`.** | ✅ Resolved |
| PL-12 | API | `QueryResponse` shape | (recommendation) | **`QueryResponse { raw: Uint8Array, kind: 'da1'|'da2'|'xtversion'|'decrpm'|'unknown' }` — carries the recognised bytes + classification; lives only in the `queries` array, never the `InputEvent` union. Consumed by RD-02; never surfaced to apps.** | ✅ Resolved |
| PL-13 | Internal | `DecoderState` shape & constants | (recommendation) | **`DecoderState` is an opaque internal carry: `{ carry: Uint8Array; paste: { active: boolean; bytes: number[]; truncated: boolean } }`, created by `createDecoderState()`. Constants: `ESC_TIMEOUT_MS = 50`, `PASTE_CAP_BYTES = 1_048_576`; carry bound reuses `RESPONSE_BUFFER_CAP`.** | ✅ Resolved |

### Resolution Notes

- **PL-2 / PL-9 (the crux):** RD-02 already demultiplexes query responses inside `runQueries`/`parseResponses` and returns `passthrough` bytes. RD-06's `decode()` is the steady-state decoder on stdin and must *also* classify query responses (AC-6), so the grammar matchers are extracted once into `capability/responses.ts` and shared (DRY). The decoder routes recognised responses into a **separate** `queries` array (PL-9) — never the app `events` union — making the AC-6 no-leak guarantee structural rather than dependent on a consumer remembering to filter.
- **PL-3:** Purity is preserved exactly as RD-02's RT-2 resolved the sync/async tension: the decoder never owns a clock. A trailing lone `ESC` is carried in state; the host's ~50 ms timer triggers `flush(state)`, which emits the Escape key. This keeps `decode()` a deterministic pure function of `(bytes, state)` so it is replayable from recorded corpora (AC-8 fuzzing).
- **PL-4 / DEF-1:** Classic xterm decoding is the go/no-go gate and covers every acceptance criterion. Full CSI-u/Kitty progressive parsing is deferred to roadmap Phase B as **DEF-1**; the `caps.keyboard.kittyFlags` branch + classic fallback are wired now so the enhancement slots in without an API change.
- **PL-11…PL-13** are the planner's recommendations, confirmed by the user's explicit "Confirm all" (per the gate's final-confirmation rule), not by silence.

### Deferred Items

- **DEF-1 (Phase B):** Full CSI-u / Kitty keyboard-protocol sequence parsing. Gated on `caps.keyboard.kittyFlags`; classic xterm decoding is the fallback shipped in this RD. No API change required to add it.
