# Ambiguity Register — App Shell (RD-05)

> **Plan**: `plans/app-shell/`
> **Status**: ✅ GATE PASSED — all items resolved, user-confirmed 2026-06-30
> **CodeOps Skills Version**: 3.1.0

This plan implements **RD-05** (`requirements/RD-05-app-shell.md`), whose *behavioral*
decisions are locked upstream as **AR-67…AR-87** in
`requirements/00-ambiguity-register.md` (AR-82…AR-87 were added by the RD-05 preflight,
`requirements/00-preflight-report-RD-05.md`) and are inherited verbatim — **not**
re-litigated here. This register captures only the **plan-level** decisions the RD left
open ("finalized in planning") plus the file/module layout, numbered `PA-NN`, with a
traceability map of the inherited decisions below.

## Plan-level decisions (PA-NN)

| PA # | Category | Question | Options Considered | Decision | Status |
|------|----------|----------|--------------------|----------|--------|
| PA-1 | Theming / core edit | Exact shape of the additive active/inactive window distinction on core's `Theme` (AR-73 deferred the shape to planning). | (a) **new `windowInactive: ThemeRole & {border,title}` sibling role** (alongside `window`/`dialog`); active frame uses `window`, background frames use `windowInactive`; (b) add inactive variant fields onto the existing `window` role (rejected by user: overloads one role with two states, less symmetric with `dialog`) | **(a) new `windowInactive` role** on `Theme` + `defaultTheme` | ✅ Resolved (user) |
| PA-2 | Architecture / menus | Where do dropdown `MenuPopup`s live, given a popup is taller than the 1-row `MenuBar` (a child would be clipped to one row by `DrawContext`) and must paint over windows? | (a) **dedicated full-screen `overlay` layer** in the app root (`[menuBar, desktop, statusLine, overlay]`, overlay top-z); popups mount there at absolute positions; menus stay **non-modal/lightweight** — the `MenuBar` (preProcess) consumes nav keys while open, a click outside closes; (b) popups as top children of the `Desktop` (rejected by user: popups mingle with the windows that raise/tile/cascade act on); (c) execView modal capture for menus (rejected: blocks the whole app, heavier than menus warrant) | **(a) dedicated overlay layer; non-modal menus** | ✅ Resolved (user) |
| PA-3 | API / lifecycle | The loop needs a viewport `Size2D` at `createApplication` time (before the host starts), but neither `Host` nor `CapabilityProfile` exposes the terminal size. Where does the initial size come from? | (a) **optional `ApplicationOptions.viewport?: Size2D`**, defaulting to the runtime's stdout `columns×rows` and falling back to `80×24` off-TTY; `run()`'s first `onResize` snaps it to the live size; (b) required `viewport` option (rejected by user: most callers want the auto-default); (c) defer loop construction to `run()` (rejected: `createApplication` must expose `loop`/`desktop` synchronously) | **(a) optional `viewport?`, auto-default stdout→80×24** | ✅ Resolved (user) |
| PA-4 | WM parameters | Concrete window minimum size, drag clamp rule, and cascade/tile geometry (AR-67/AR-74 left the values to planning). | (a) **compact preset** — min **10w×3h** (border + close/zoom boxes + ≥1 interior cell); drag clamp = the whole title-bar row stays on the desktop and ≥1 frame column stays inside (never fully off); cascade stagger **+1 row / +2 cols** per window from top-left; tile clamps each cell to the min (cells may overflow the desktop edge, consistent with RD-02 overflow AR-28); (b) roomier preset 16×4 / half-title-visible / +2/+2 (rejected by user) | **(a) compact preset** | ✅ Resolved (user) |
| PA-5 | Behavioral / capture seam (PF-001/AR-82) | Exact semantics of the additive `setCapture`/`releaseCapture` loop seam. | (a) **while a capture target is set, `hitTestRoute` short-circuits**: every mouse/wheel event routes to the target's `onEvent` with target-local `ev.local`, **focus-on-click is suppressed**, and no hit-test/bubble runs, until `releaseCapture()`; (b) capture only `move`/`drag`/`up` and still hit-test `down` (rejected: a `down` mid-gesture would mis-route; the gesture owns the pointer wholesale until release) | **(a) full capture to the target until release; focus-on-click suppressed** | ✅ Resolved (dominant) |
| PA-6 | Behavioral / onFrame seam (PF-003/AR-84) | Exact timing + payload of the additive `onFrame` hook. | (a) **`EventLoopOptions.onFrame?(buffer: ScreenBuffer)`** fired at the **end of every `runTick`** (after the single coalesced `flush()`), on `resize` (after its flush), and once after `mount` — so `run()` pushes **every** frame (sync or async, e.g. an `endModal` resolved from a timer) to `host.render`; (b) fire only after `dispatch` (rejected: async `endModal`/command frames never paint — the PF-003 failure) | **(a) onFrame after every flush (tick/resize/mount)** | ✅ Resolved (dominant) |
| PA-7 | Architecture / wiring | How does the `Desktop` (and its windows' frames) reach the loop to capture the pointer + emit commands, given views hold no loop reference? | (a) **`Application` injects a loop handle into the `Desktop`** at construction — a narrow seam `{ setCapture, releaseCapture, emitCommand, isCommandEnabled, focusView }`; a `Frame` reaches its `Desktop` via `window.parent`; (b) give every `View` a loop back-reference (rejected: couples the whole view tree to the loop; RD-03/04 deliberately kept views loop-agnostic) | **(a) Application injects a narrow loop seam into the Desktop** | ✅ Resolved (dominant) |
| PA-8 | Architecture / Frame | Is `Frame` a `View` subclass (the RD's indicative sketch) or a Window-internal helper? | (a) **Window-internal drawing+geometry helper** (a module, not a flex child): `Window.draw` renders the border/title/number/close-box/zoom-box/resize-corner; `Window` sets layout `padding:1` so content children inset inside the border; `Window.onEvent` maps the frame hit-zones (close/zoom/move-start/resize-start) using the helper's geometry; avoids the "Frame-as-flex-child vs full-rect overlay" conflict; (b) `Frame` as a full-window `View` child painted under the content (rejected: must be kept out of the flex flow and manually positioned each reflow — more moving parts for no gain). **Refines RD-05's indicative "Frame extends View" wording (signatures were indicative).** | **(a) Window-internal Frame helper; Window owns chrome + hit-zones** | ✅ Resolved (dominant) |
| PA-9 | Behavioral / menus | How is multi-level menu navigation modelled? | (a) **a single menu controller owned by `MenuBar`** holds the open-popup **path** (top-level → nested) + a highlighted index per level; `MenuPopup`s are presentational views in the overlay rendered from that state; `MenuBar.preProcess.onEvent` runs the state machine (F10/Alt-hotkey/title-click open; ↑↓ move skipping separators/disabled; Enter activate; ←→ switch top-level; Esc close one level; item hotkey activates) consuming via `ev.handled`; opening **saves focus**, closing **restores** it (reuse the focus save/restore pattern); (b) each popup owns its own key handling as a focusable view (rejected: distributes the state machine, complicates nested ←→ and Esc-one-level) | **(a) single MenuBar-owned controller; presentational popups** | ✅ Resolved (dominant) |
| PA-10 | Behavioral / WM state | Where is the active drag/resize/zoom gesture state held? | (a) **on the `Desktop`** — `{ kind: 'move'|'resize', target: Window, anchor }` for an active gesture, plus each `Window`'s saved restored-geometry for zoom; the captured `onEvent` (PA-5) mutates `target.bounds` + calls `invalidateLayout()` so RD-03 reflow re-insets content → one coalesced frame/tick (AR-54); (b) on each `Window` (rejected: the Desktop owns z-order/clamp/the loop seam — gesture logic belongs with the manager) | **(a) gesture state on the Desktop** | ✅ Resolved (dominant) |
| PA-11 | Architecture / files | Source + test layout for RD-05. | (a) **new granular modules** `packages/ui/src/{app,desktop,window,menu,status}/` (each a barrel `index.ts` + concern files ≤ 500 lines), explicit named re-exports added to `src/index.ts`; the additive core role in `packages/core/src/engine/color/theme.ts` (+ `color/index.ts` already re-exports `Theme`); the additive loop seams in `packages/ui/src/event/{types.ts, event-loop.ts, hit-test.ts}`; tests `packages/ui/test/app-shell.<concern>.{spec,impl}.test.ts` + a core `theme-windowinactive.spec.test.ts` + `packages/examples/shell-demo/` e2e; (b) one `app.ts` (rejected: an XL subsystem blows past 500 lines) | **(a) granular `src/{app,desktop,window,menu,status}/` split** | ✅ Resolved (dominant) |
| PA-12 | Behavioral / commands | How are the standard commands wired? | (a) **`Application` registers** `quit/close/zoom/next/prev/cascade/tile` (via the loop's command registry / `opts.commands` hint) and binds **`'quit'` → terminate `run()`** (resolve the exit code); the WM commands (`zoom/next/prev/cascade/tile`) are handled by the `Desktop`'s post-process `onEvent` on the matching `CommandEvent`; menu/status items emit by `Commands` constant; an app-supplied `keymap` is optional (AR-62); (b) bind WM commands in each Window (rejected: cascade/tile/next/prev are desktop-wide) | **(a) Application registers; Desktop handles WM commands; quit ends run()** | ✅ Resolved (dominant) |
| PA-13 | Scope / demos | Ship a runnable `demo:shell` + a real-TTY demo (AR-70)? | (a) **both** — `packages/examples/shell-demo/` headless scripted-`dispatch` walkthrough (open 2–3 windows → raise/drag/zoom one → drop a menu + fire a command → status line, printing ASCII frames via `loop.renderRoot.buffer()`), run via a `demo:shell` `tsx` script + a probe-style e2e (deterministic, CI-able, mirrors `demo:events`); **plus** a real-TTY interactive demo (manual `run()` proof, like `demo:resize`, **not** a CI gate); (b) headless only (rejected: never exercises live `createHost` at the demo layer — AR-70) | **(a) both `demo:shell` + real-TTY demo** | ✅ Resolved (AR-70) |
| PA-14 | Testing / oracles | Spec-test oracle set + how the live-TTY paths are tested headlessly. | (a) **ST-01…ST-22 ↔ AC-1…AC-22**; the `run()` lifecycle, **quit→exit code**, **restore-on-throw**, and **suspend/resume** are driven by an **injected fake `RuntimeAdapter`** (mirroring core's host tests — no real TTY, deterministic); WM/menu/status behavior via synthetic `dispatch()` + buffer assertions; one-frame-per-interaction via an `onFrame`/flush counter; the real-TTY demo is **manual, not gated**; (b) gate the real-TTY demo (rejected: not deterministic for CI — AR-70) | **(a) ST-01…ST-22; fake-runtime lifecycle tests; real-TTY ungated** | ✅ Resolved (PA-13/AR-70) |

## Plan preflight decisions (PA-15…PA-19 — added after `00-preflight-report.md`)

> These resolve the plan-preflight findings PF-01…PF-06. Decision record:
> [00-pf01-scope-addition.md](00-pf01-scope-addition.md). Realized as **Phase 0** (03-00) + spec edits.

| PA # | Category | Question | Options Considered | Decision | Status |
|------|----------|----------|--------------------|----------|--------|
| PA-15 | Layout / WM (PF-01,02) | The WM mutates `window.bounds` + `invalidateLayout()`, but RD-03 reflow overwrites every `bounds` from the **pure-flex** `layout()` (`reflow.ts:30-33`) — so window geometry is clobbered and overlapping/free-floating windows + a full-viewport overlay are inexpressible. How are free-positioned views realized? | (a) **additive RD-02 `position:'absolute'` + `rect`** on `LayoutProps`: an absolute child leaves flex flow, is placed at its parent-content-relative rect, and its children flow within it; the WM mutates `window.layout.rect` (reflow re-honors it); the overlay is an absolute full-viewport layer; (b) a Desktop-custom reflow that lays out windows itself outside the flex pass (rejected: a bespoke reflow seam, less reusable, doesn't help the overlay); (c) give views absolute screen bounds bypassing reflow (rejected: reflow always rewrites `bounds`; no opt-out exists) | **(a) RD-02 additive absolute placement** (intra-package; Phase 0) | ✅ Resolved (preflight) |
| PA-16 | Draw / theme (PF-03) | `DrawContext.color(role)` returns `{fg,bg}` only (`theme-style.ts:15-17`); the desktop `pattern` and frame `border`/`title` colors are unreachable, yet AR-80/AR-73 need them. | (a) **additive RD-03 `DrawContext.role<K>(name): Theme[K]`** raw-role accessor; `Desktop.draw` overrides the space-fill to paint the pattern; `drawFrame` reads `border`/`title`; (b) inject the active `Theme` into Desktop/Window via the loop seam (rejected: widens the seam; views already get a `ctx`); (c) hardcode `'░'` + fg-only borders (rejected: ignores `theme.desktop.pattern`/`window.border`, breaks custom themes, leaves the PA-1 role shape cosmetic) | **(a) additive `DrawContext.role`** (intra-package; Phase 0) | ✅ Resolved (preflight) |
| PA-17 | Sequencing (PF-01…) | When do the PA-15/PA-16 foundation extensions land relative to the app-shell phases? | (a) **a spec-first Phase 0 before Phase 1** (FX-01…FX-05 RED → implement → GREEN), so every later phase builds on a real surface; (b) interleave them into Phase 3/4 (rejected: Phase 3 spec tests can't even construct overlap scenarios without the primitive) | **(a) Phase 0, spec-first, before Phase 1** | ✅ Resolved (preflight) |
| PA-18 | Loop seam (PF-04) | `onFrame` was specified on `EventLoopOptions` (construction-time), but `run()` sets `loop.onFrame = host.render` after the host exists — a contradiction (the host doesn't exist at `createApplication`). | (a) **a settable `onFrame?` member on `EventLoop`** (default `undefined`; `run()` assigns it; fired after every flush); (b) a construction-time sink indirection `createApplication` owns and `run()` reassigns (rejected: more indirection for the same effect; the member is the simpler public shape) | **(a) settable `EventLoop.onFrame` member** | ✅ Resolved (preflight) |
| PA-19 | Menus (PF-06) | Mouse events bypass the pre-process sweep (RD-04 routes a click to the single top-most hit view — `dispatch.ts:111-115`), so a pre-process `MenuBar` never sees an outside click → click-outside-close is unimplementable as specified. | (a) **a full-viewport transparent catcher** view mounted in the overlay (below popups, above all else) while a menu is open; its `onEvent` closes the menu on an outside mouse-down; (b) use the pointer-capture seam for the open-menu lifetime (rejected: capture is documented drag/resize-only and suppresses focus-on-click; reconciling that is heavier than a catcher) | **(a) overlay click-catcher** | ✅ Resolved (preflight) |

> **Note (PF-05/07/08/09 — no decision needed):** `Window.focusable = true` (raise/`activeWindow()`
> correctness), single-arg `createHost`, count reconciliation, and dropping the inert `onResume`
> nudge are straight corrections folded into the relevant docs/phases.

## Iteration-2 plan-preflight decisions (PA-20…PA-22 — added after the iteration-2 re-scan)

> Resolve iteration-2 findings PF-10…PF-14 (`00-preflight-report.md` → Iteration 2). User-accepted
> 2026-06-30. All chosen Option A; integrated into 03-01/03-04/07/99 (+ 03-00/00-pf01 text fixes).

| PA # | Category | Question | Options Considered | Decision | Status |
|------|----------|----------|--------------------|----------|--------|
| PA-20 | Hit-test / overlay (PF-10) | The always-present full-viewport `overlay` Group wins the top-z hit-test for **every** point even when empty (`hit-test.ts:55-67` returns a childless group via `contains`; mouse delivery is single-target, no bubble), so it swallows all clicks and starves window/desktop/status/menu-title interaction. How is the empty overlay made inert? | (a) **menu controller toggles `overlay.state.visible`** — `false` when empty (omitted from reflow + hit-test, `reflow.ts:43`/`hit-test.ts:50`), `true` (set first) while the catcher+popups are mounted; (b) toggle `overlay.state.disabled` (rejected: `disabled` carries focus-subtree + greyed semantics — wrong meaning); (c) keep the overlay rect zero when empty, full-viewport when open (rejected: fiddlier — child popups place against a zero content box and clip to zero until expanded) | **(a) visibility-gated overlay** | ✅ Resolved (preflight iter-2) |
| PA-21 | Sequencing / skeletons (PF-12) | Phase 2's `createApplication` composes/types `Desktop` (+ optional `MenuBar`/`StatusLine`), but those classes are only built in Phases 3/4/5 — so Phase 2 can't reach its ST-01…05 GREEN gate (`instanceof Desktop` fails; the chrome types don't exist). | (a) **Phase 1 (T1.6) seeds minimal constructable class skeletons** — `Desktop extends Group` (pattern `draw` + `attachLoop` stub), `MenuBar`/`StatusLine extends View` (no-op draw); Phases 3/4/5 **extend** them; `Window` stays Phase 3 (not referenced by `createApplication`); (b) move the lifecycle-GREEN gate past Phase 3 (rejected: breaks per-phase spec-first discipline, leaves Phase 2 without a clean exit) | **(a) Phase-1 class skeletons, extended later** | ✅ Resolved (preflight iter-2) |
| PA-22 | Testing / oracle (PF-11) | ST-04/AC-4 "a **handler** throws → restore (`host.stop()`)" contradicts the shipped RD-04 isolation: a view `onEvent`/`draw` throw is caught+logged by `deliver` (AR-66, `event-loop.ts:209-215`) and never reaches `run()`. | (a) **re-spec ST-04 to the escaping-throw path** (fake `onUncaughtException` / an error in `host.start()`/first-frame push → restore), and document handler throws as isolated (non-restoring, per 03-01 error table); flag AC-4's "a handler" wording in RD-05 for a one-line upstream clarification; (b) make the loop re-raise handler throws (rejected: regresses AR-66 — one bad widget could wedge/kill the app) | **(a) re-spec ST-04 to the escaping path** | ✅ Resolved (preflight iter-2) |

> **PF-13/PF-14 (text fixes — no decision):** the Phase-0 pseudocode `layoutContainer(child, rect.size, …)`
> → `{ width: rect.width, height: rect.height }` (03-00 §A.3, 00-pf01 §A.2), and the FX-05 test
> filename `draw-context.role.spec.test.ts` → `view.drawcontext-role.spec.test.ts` (00-pf01) to match
> 03-00/07/99.

> **Upstream follow-up (PA-22) — applied:** RD-05 AC-4 (`requirements/RD-05-app-shell.md`) was reworded
> (user-authorized 2026-06-30) so restore triggers on an **escaping** throw (uncaught exception via the
> host backstop, or an error in `run()`'s lifecycle) or signal, and a view-handler `onEvent`/`draw` throw
> is explicitly noted as **isolated** (AR-66) — not a restore trigger. Plan ST-04 (PA-22) now matches it.

> **Cross-package surface this time.** RD-05 adds exactly **one** cross-package edit — the
> additive `windowInactive` role on `@jsvision/core`'s `Theme` + `defaultTheme` (PA-1/AR-73). It also
> adds **four additive intra-package edits** (all within `packages/ui`, so AC-21 holds): two RD-02/RD-03
> contract extensions — `LayoutProps` absolute placement (PA-15) and `DrawContext.role` (PA-16) — and
> two seams on the RD-04 `EventLoop` it composes: pointer capture (`setCapture`/`releaseCapture`,
> PA-5/AR-82) and the settable `onFrame` member (PA-6/PA-18/AR-84). The loop is
> **composed, not re-shaped**; everything else builds on the existing public surface of
> `@jsvision/core` (`createHost`/`Host`/`RuntimeAdapter`, `serialize`/`ScreenBuffer`, `Keymap`,
> `Theme`/`defaultTheme`, `Logger`) and RD-01…RD-04 (`View`/`Group`, `RenderRoot`, `EventLoop`).

## Inherited requirements decisions (RD-05 AR-NN) — traceability

| AR # | Locked decision (RD-05) | Where realized in this plan |
|------|-------------------------|------------------------------|
| AR-67 | Full interactive WM (raise · drag · zoom · cascade/tile · Alt-N · close) | 03-02 (Desktop) |
| AR-68 | Full nested menus (F10/Alt-hotkey/click, ↑↓/Enter/←→/Esc nav, tilde accelerators, enable/disable) | 03-04 (Menus) · PA-9 |
| AR-69 | `ScrollBar`/`Scroller` deferred to RD-06 | 01-requirements (Out of scope) |
| AR-70 | Demos = headless `demo:shell` + real-TTY interactive | 03-05 · PA-13 |
| AR-71 | `'quit'`→`Promise<number>` exit code; restore on every path; suspend/resume | 03-01 (Application/run) · PA-12 |
| AR-72 | Static StatusLine + enable/disable greying | 03-05 (StatusLine) |
| AR-73 | Additive active/inactive window role on core `Theme` | 03-05 · PA-1 |
| AR-74 | Free drag-resize (SE corner) + zoom | 03-02 · PA-5/PA-10 |
| AR-75 | Composition over inheritance (`createApplication`) | 03-01 |
| AR-76 | `Commands` constants (`resize`/`move` dropped per PF-004) | 03-05 · PA-12 |
| AR-77 | Tilde `~X~` hotkey marker | 03-04 · 03-05 |
| AR-78 | Raise = click-to-top + focus; active = top-most focused | 03-02 |
| AR-79 | Modal windows reuse `execView`/`endModal`; rich `Dialog` → RD-06 | 03-01 · 01-requirements (Out of scope) |
| AR-80 | Desktop background = core `desktop` role + pattern | 03-02 |
| AR-81 | Pure TS, zero deps, explicit named re-exports | 03-05 · PA-11 |
| AR-82 | Pointer-capture seam (additive loop) | 03-05 (seams) · PA-5 |
| AR-83 | Host owns SIGCONT mode-reassert + repaint; `onResume` notify-only | 03-01 (suspend/resume) |
| AR-84 | `onFrame` hook (additive loop) | 03-05 (seams) · PA-6 |
| AR-85 | `resize`/`move` dropped from `Commands` | 03-05 · PA-12 |
| AR-86 | `run()` exit code: `0` default / `quit` arg | 03-01 · PA-12 |
| AR-87 | Cascade/tile clamp-to-min + un-zoom-first + degenerate counts | 03-02 · PA-4 |
